services:
  quiz:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_PATH: /app/data/quiz.db
    volumes:
      - quiz_data:/app/data
    restart: unless-stopped

volumes:
  quiz_data:
